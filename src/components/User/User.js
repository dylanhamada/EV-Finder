import React from 'react';

import styles from './User.module.css';

const User = props => {
    return(
        <div className={styles.User}>

        </div>
    );
};

export default User;
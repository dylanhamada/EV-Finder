import React from "react";

import Selector from "../Selector/Selector";

const One = (props) => {
  return (
    <div>
      <Selector which="one" />
    </div>
  );
};

export default One;

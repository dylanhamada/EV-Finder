import React from 'react';

import { Link } from 'react-router-dom';

const Intro = (props) => {
    return (
        <React.Fragment>
            <p>Answer a few simple questions and get matched to an electric vehicle that meets your needs. The whole process should take less than five minutes.</p>
            
        </React.Fragment>
    );
};

export default Intro;